package domain.flight;

public class FlightEnums {
    public enum TravelClass { ECONOMY, PREMIUM_ECONOMY, BUSINESS, FIRST }
    public enum TimeSlot { MORNING, AFTERNOON, EVENING, NIGHT }
    public enum PriceRange { BUDGET, MODERATE, PREMIUM }
    public enum FlightStatus { ON_TIME, DELAYED, CANCELLED, BOARDING }
}